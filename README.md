## Ruby

```
cd ruby
gem install bundler
bundle install
bundle exec thin -R config.ru start
open http://localhost:3000
```

## Node.js

```
cd node
npm install
npm start
open http://localhost:8000
```
